import { WORKSPACES_DOM, WORKSPACES_LIST_DOM } from './workspace.dom';

Cypress.Commands.add('workspaces', (should = true) => {
  // TODO: Describe workspaces commands
  // cy.get(WORKSPACES_DOM.something).get(something);
});

Cypress.Commands.add('workspaces-list', (should = true) => {
  // TODO: Describe workspaces-list commands
  // cy.get(WORKSPACES_LIST_DOM.something).get(something);
});
