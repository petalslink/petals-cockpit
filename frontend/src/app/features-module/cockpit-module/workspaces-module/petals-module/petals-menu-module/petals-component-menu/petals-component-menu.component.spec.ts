/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ComponentMenuComponent } from './petals-component-menu.component';

describe('Component: ComponentMenu', () => {
  it('should create an instance', () => {
    let component = new ComponentMenuComponent();
    expect(component).toBeTruthy();
  });
});
