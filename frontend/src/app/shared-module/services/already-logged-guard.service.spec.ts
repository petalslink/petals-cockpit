/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AlreadyLoggedGuardService } from './already-logged-guard.service';

describe('Service: AlreadyLoggedGuard', () => {
  // beforeEach(() => {
  //   TestBed.configureTestingModule({
  //     providers: [AlreadyLoggedGuardService]
  //   });
  // });
  //
  // it('should ...', inject([AlreadyLoggedGuardService], (service: AlreadyLoggedGuardService) => {
  //   expect(service).toBeTruthy();
  // }));
});
