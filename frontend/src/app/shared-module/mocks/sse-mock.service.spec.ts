/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { SseMockService } from './sse-mock.service';

describe('Service: SseMock', () => {
  // beforeEach(() => {
  //   TestBed.configureTestingModule({
  //     providers: [SseMockService]
  //   });
  // });
  //
  // it('should ...', inject([SseMockService], (service: SseMockService) => {
  //   expect(service).toBeTruthy();
  // }));
});
