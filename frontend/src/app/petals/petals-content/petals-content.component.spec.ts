/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PetalsMenuToolbarComponent } from './petals-content.component';

describe('Component: PetalsMenuToolbar', () => {
  it('should create an instance', () => {
    let component = new PetalsMenuToolbarComponent();
    expect(component).toBeTruthy();
  });
});
